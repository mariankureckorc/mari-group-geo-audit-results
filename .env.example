# Password required to view the GEO Audit report.
# Set the real value locally in .env.local (gitignored) and in
# Vercel -> Project -> Settings -> Environment Variables (all environments).
SITE_PASSWORD=change-me
